# Required for `docker compose` (build + app container). Copy to `.env`.
PORT=3000
REDIS_URL=redis://localhost:6380
API_STATIC_KEY=dev-static-token
# Optional: https://www.exchangerate-api.com/ — if empty, open.er-api.com (see their terms)
EXCHANGE_RATE_API_KEY=
